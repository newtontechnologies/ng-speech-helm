apiVersion: v1
kind: Service
metadata:
  name: {{ include "ng-v2t.fullname" . }}
  labels:
    {{- include "ng-v2t.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ng-v2t.selectorLabels" . | nindent 4 }}
