apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ng-v2t.fullname" . }}-test-connection"
  labels:
    {{- include "ng-v2t.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ng-v2t.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
