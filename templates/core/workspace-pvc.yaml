{{ with .Values.persistence.workspace }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{ include "ng-v2t.workspacePvc" $ }}"
spec:
  accessModes:
    {{- toYaml .accessModes | nindent 4 }}
  resources:
    requests:
      storage: {{ .storageSize }}
  storageClassName: "{{ default $.Values.persistence.storageClassName .storageClassNam }}"
{{ end}}
