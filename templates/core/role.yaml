apiVersion: rbac.authorization.k8s.io/v1
  kind: Role
  metadata:
    name: {{ include "ng-v2t.coreRoleName" . }}
  rules:
    - apiGroups: [ "" ]
      resources: [ "pods" ]
      verbs: [ "get", "watch", "list" ]
